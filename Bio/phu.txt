        .skills-section {
            margin-bottom: 30px;
            padding: 0 20px;
        }

        /* Tiêu đề được canh giữa và đổ màu Gradient cực cháy */
        .section-title {
            font-size: 1.4rem;
            font-weight: 800;
            text-align: center;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
            background: linear-gradient(to right, var(--gradient-start), var(--gradient-end));
            -webkit-background-clip: text;
            color: transparent;
        }

        .section-title {
            text-align: center;
            font-size: 0.95rem; font-weight: 700; color: var(--primary);
            letter-spacing: 2px;
            margin-bottom: 25px;
            display: flex; align-items: center; justify-content: center; gap: 15px;
        }
        


.section-title {
            text-align: center;
            font-size: 0.95rem; font-weight: 700; color: var(--primary);
            letter-spacing: 2px;
            margin-bottom: 25px;
            display: flex; align-items: center; justify-content: center; gap: 15px;
        }
        
        .section-title::before, .section-title::after {
            content: ''; height: 2.5px; width: 40px;
            background: linear-gradient(to right, transparent, var(--primary));
            border-radius: 2px;
        }
        .section-title::after { background: linear-gradient(to left, transparent, var(--primary)); }


màu 

:root {
    /* Nền màu xanh sky cực nhạt, tạo cảm giác trong trẻo */
    --bg-color: #f4f8fc; 
    --card-bg: #ffffff;
    
    /* Màu chủ đạo: Xanh Sky tươi sáng */
    --primary: #0ea5e9; 
    
    /* Gradient chuyển mượt từ Hồng Nhạt sang Xanh Sky */
    --gradient-start: #f472b6; /* Hồng nhạt đào */
    --gradient-end: #38bdf8;   /* Xanh sky dịu */
    
    /* Màu chữ xám xanh đậm để không bị xỉn màu */
    --text-main: #1e293b;
    --text-sub: #64748b;
    
    /* Đổ bóng nhẹ nhàng pha chút ánh xanh sky */
    --shadow-light: rgba(255, 255, 255, 0.9);
    --shadow-dark: rgba(14, 165, 233, 0.06);
    
    --transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

body.dark-mode {
    /* Nền tối màu xanh dương đêm (Midnight Navy) cực sâu và sang */
    --bg-color: #0b0f19; 
    --card-bg: #161f30;
    
    /* Tăng độ sáng của màu xanh sky một chút để nổi bật trên nền tối */
    --primary: #38bdf8; 
    
    /* Giữ nguyên gradient nhưng hiển thị lung linh hơn trên nền tối */
    --gradient-start: #f472b6;
    --gradient-end: #38bdf8;
    
    /* Chữ trắng sáng và xám xanh nhạt */
    --text-main: #f8fafc;
    --text-sub: #94a3b8;
    
    --shadow-light: rgba(255, 255, 255, 0.01);
    --shadow-dark: rgba(0, 0, 0, 0.4);
}
